---
layout: base.njk
title: AI Prompts
---

<div class="content-type-header">
    <h1>AI Prompts</h1>
    <p class="description">Browse and search through our collection of AI prompts for various use cases.</p>
</div>

<div class="content-list">
    {% if collections.prompts.length > 0 %}
        {% for prompt in collections.prompts %}
            <article class="content-item">
                <h2><a href="{{ baseUrl }}{{ prompt.url }}">{{ prompt.data.title }}</a></h2>
                <p>{{ prompt.data.description }}</p>
                <div class="metadata">
                    <span class="date">{{ prompt.date | date("yyyy-MM-dd") }}</span>
                    {% if prompt.data.tags %}
                        <div class="tags">
                            {% for tag in prompt.data.tags %}
                                <span class="tag">{{ tag }}</span>
                            {% endfor %}
                        </div>
                    {% endif %}
                </div>
            </article>
        {% endfor %}
    {% else %}
        <article class="content-item">
            <h2>No Prompts Yet</h2>
            <p>Check back soon for our collection of AI prompts!</p>
        </article>
    {% endif %}
</div> 