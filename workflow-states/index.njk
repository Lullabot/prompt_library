---
layout: base.njk
title: Workflow States
---

<div class="hero">
    <h1>Workflow States</h1>
    <p>Learn about different workflow states and their implementations.</p>
</div>

<div class="content-grid">
    {% if collections.workflowStates.length > 0 %}
        {% for state in collections.workflowStates %}
            <section class="content-card">
                <h2>{{ state.data.title }}</h2>
                <p>{{ state.data.description }}</p>
                <div class="tags">
                    {% for tag in state.data.tags %}
                        <span class="tag">{{ tag }}</span>
                    {% endfor %}
                </div>
                <a href="{{ baseUrl }}{{ state.url }}" class="button">View State</a>
            </section>
        {% endfor %}
    {% else %}
        <section class="content-card">
            <h2>No States Yet</h2>
            <p>Check back soon for our collection of workflow states!</p>
        </section>
    {% endif %}
</div> 