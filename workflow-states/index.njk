---
layout: base.njk
title: Workflow States
---

<div class="content-type-header">
    <h1>Workflow States</h1>
    <p class="description">Explore workflow states and processes for different project types.</p>
</div>

<div class="content-list">
    {% if collections.workflowStates.length > 0 %}
        {% for state in collections.workflowStates %}
            <article class="content-item">
                <h2><a href="{{ baseUrl }}{{ state.url }}">{{ state.data.title }}</a></h2>
                <p>{{ state.data.description }}</p>
                <div class="metadata">
                    <span class="date">{{ state.date | date("yyyy-MM-dd") }}</span>
                    {% if state.data.tags %}
                        <div class="tags">
                            {% for tag in state.data.tags %}
                                <span class="tag">{{ tag }}</span>
                            {% endfor %}
                        </div>
                    {% endif %}
                </div>
            </article>
        {% endfor %}
    {% else %}
        <article class="content-item">
            <h2>No Workflow States Yet</h2>
            <p>Check back soon for our collection of workflow states!</p>
        </article>
    {% endif %}
</div> 