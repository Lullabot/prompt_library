---
layout: base.njk
title: Project Configurations
---

<div class="content-type-header">
    <h1>Project Configurations</h1>
    <p class="description">Browse through our collection of project configuration templates and examples.</p>
</div>

<div class="content-list">
    {% if collections.projectConfigs.length > 0 %}
        {% for config in collections.projectConfigs %}
            <article class="content-item">
                <h2><a href="{{ baseUrl }}{{ config.url }}">{{ config.data.title }}</a></h2>
                <p>{{ config.data.description }}</p>
                <div class="metadata">
                    <span class="date">{{ config.date | date("yyyy-MM-dd") }}</span>
                    {% if config.data.tags %}
                        <div class="tags">
                            {% for tag in config.data.tags %}
                                <span class="tag">{{ tag }}</span>
                            {% endfor %}
                        </div>
                    {% endif %}
                </div>
            </article>
        {% endfor %}
    {% else %}
        <article class="content-item">
            <h2>No Project Configurations Yet</h2>
            <p>Check back soon for our collection of project configurations!</p>
        </article>
    {% endif %}
</div> 