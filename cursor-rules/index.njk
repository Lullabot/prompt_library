---
layout: base.njk
title: Cursor Rules
---

<div class="content-type-header">
    <h1>Cursor Rules</h1>
    <p class="description">Explore cursor rules for different development environments and workflows.</p>
</div>

<div class="content-list">
    {% if collections.cursorRules.length > 0 %}
        {% for rule in collections.cursorRules %}
            <article class="content-item">
                <h2><a href="{{ baseUrl }}{{ rule.url }}">{{ rule.data.title }}</a></h2>
                <p>{{ rule.data.description }}</p>
                <div class="metadata">
                    <span class="date">{{ rule.date | date("yyyy-MM-dd") }}</span>
                    {% if rule.data.tags %}
                        <div class="tags">
                            {% for tag in rule.data.tags %}
                                <span class="tag">{{ tag }}</span>
                            {% endfor %}
                        </div>
                    {% endif %}
                </div>
            </article>
        {% endfor %}
    {% else %}
        <article class="content-item">
            <h2>No Cursor Rules Yet</h2>
            <p>Check back soon for our collection of cursor rules!</p>
        </article>
    {% endif %}
</div> 