<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>{{ title }}</title>
  <link rel="stylesheet" href="{{ baseUrl }}/assets/css/styles.css">
  <script src="https://cdn.jsdelivr.net/npm/marked/marked.min.js"></script>
  <script>
    // Configure marked.js
    marked.setOptions({
      breaks: true,
      gfm: true,
      headerIds: false,
      mangle: false
    });
  </script>
  <style>
    .container {
      max-width: 1200px;
      margin: 0 auto;
      padding: 0 1rem;
    }
    
    .site-header {
      background-color: #f8fafc;
      padding: 1rem 0;
      margin-bottom: 2rem;
      border-bottom: 1px solid #e2e8f0;
    }
    
    .main-nav {
      display: flex;
      justify-content: space-between;
      align-items: center;
    }
    
    .logo {
      font-size: 1.5rem;
      font-weight: 700;
      color: #2d3748;
      text-decoration: none;
    }
    
    .nav-links {
      display: flex;
      gap: 2rem;
      list-style: none;
      margin: 0;
      padding: 0;
    }
    
    .nav-links a {
      color: #4a5568;
      text-decoration: none;
      font-weight: 500;
      padding: 0.5rem 0;
      position: relative;
    }
    
    .nav-links a:hover {
      color: #2b6cb0;
    }
    
    .nav-links a::after {
      content: '';
      position: absolute;
      bottom: 0;
      left: 0;
      width: 0;
      height: 2px;
      background-color: #2b6cb0;
      transition: width 0.2s;
    }
    
    .nav-links a:hover::after {
      width: 100%;
    }
    
    .site-footer {
      margin-top: 4rem;
      padding: 2rem 0;
      border-top: 1px solid #e2e8f0;
      text-align: center;
      color: #718096;
    }

    @media (max-width: 768px) {
      .main-nav {
        flex-direction: column;
        gap: 1rem;
      }
      
      .nav-links {
        flex-direction: column;
        align-items: center;
        gap: 1rem;
      }
    }
  </style>
</head>
<body>
  <header class="site-header">
    <div class="container">
      <nav class="main-nav">
        <a href="{{ '/' | url }}" class="logo">Prompt Library</a>
        <ul class="nav-links">
          <li><a href="{{ '/project-management' | url }}">Project Management</a></li>
          <li><a href="{{ '/development' | url }}">Development</a></li>
          <li><a href="{{ '/sales-marketing' | url }}">Sales & Marketing</a></li>
          <li><a href="{{ '/content-strategy' | url }}">Content Strategy</a></li>
          <li><a href="{{ '/design' | url }}">Design</a></li>
        </ul>
      </nav>
    </div>
  </header>

  <main class="container">
    {% include "search.njk" %}
    {{ content | safe }}
  </main>

  <footer class="site-footer">
    <div class="container">
      <p>&copy; {{ "now" | date: "%Y" }} Prompt Library. All rights reserved.</p>
    </div>
  </footer>
</body>
</html> 