<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>{{ title }}</title>
  <link rel="stylesheet" href="{{ baseUrl }}/assets/css/styles.css">
  <script src="https://cdn.jsdelivr.net/npm/marked/marked.min.js"></script>
  <script>
    // Configure marked.js
    marked.setOptions({
      breaks: true,
      gfm: true,
      headerIds: false,
      mangle: false
    });
  </script>
</head>
<body>
  <header class="site-header">
    <div class="container">
      <nav class="main-nav">
        <a href="{{ baseUrl }}/" class="logo">Prompt Library</a>
        <ul class="nav-links">
          {% for discipline in ['project-management', 'development', 'sales-marketing', 'content-strategy', 'design', 'quality-assurance'] %}
            <!-- DEBUG: Nav loop discipline: {{ discipline }} -->
            <li>
              {% set hasAnyContent = false %}
              {% for type in ['prompts', 'rules', 'project-configs', 'workflow-states'] %}
                <!-- DEBUG: Nav check: {{ discipline }} | {{ type }} -->
                {% if discipline | hasContent(type) %}
                  {% set hasAnyContent = true %}
                {% endif %}
              {% endfor %}
              <!-- DEBUG: Nav hasAnyContent for {{ discipline }}: {{ hasAnyContent }} -->
              <a href="{{ baseUrl }}/{{ discipline }}" class="nav-item {% if not hasAnyContent %}nav-empty{% endif %}">
                {% if discipline == 'quality-assurance' %}Quality Assurance{% else %}{{ discipline | replace('-', ' ') | title }}{% endif %}
                <span class="content-indicator" aria-label="{% if hasAnyContent %}Contains content{% else %}No content{% endif %}"></span>
              </a>
            </li>
          {% endfor %}
          <li><a href="{{ baseUrl }}/help">Help</a></li>
          <li><a href="{{ baseUrl }}/contributing">Contributing</a></li>
        </ul>
      </nav>
    </div>
  </header>

  <main class="container">
    {% include "search.njk" %}
    {{ content | safe }}
  </main>

  <footer class="site-footer">
    <div class="container">
      <p>&copy; {{ "now" | date("yyyy") }} Prompt Library</p>
      <div class="footer-links">
        <a href="{{ baseUrl }}/about">About</a>
        <a href="{{ baseUrl }}/contributing">Contributing</a>
        <a href="{{ baseUrl }}/help">Help</a>
      </div>
    </div>
  </footer>
</body>
</html> 